import 'package:google_maps_flutter/google_maps_flutter.dart';

class Places {

  String fullAddress;
  String mainAddress;
  String secondaryAddress;
  String placeID;

  Places({this.fullAddress, this.mainAddress, this.secondaryAddress, this.placeID});
}